<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!-- Base application theme -->
    <style name="Theme.Stride" parent="Theme.MaterialComponents.DayNight.NoActionBar">
        <!-- Customize your theme here -->
        <item name="colorPrimary">@color/primaryColor</item>
        <item name="colorPrimaryVariant">@color/primaryColor</item>
        <item name="colorOnPrimary">@android:color/white</item>
        <item name="colorSecondary">@color/primaryColor</item>
        <item name="colorOnSecondary">@android:color/white</item>
    </style>

    <!-- AppBar overlay theme -->
    <style name="Theme.Stride.AppBarOverlay" parent="ThemeOverlay.MaterialComponents.Dark.ActionBar" />

    <!-- Popup overlay theme -->
    <style name="Theme.Stride.PopupOverlay" parent="ThemeOverlay.MaterialComponents.Light" />
</resources>
